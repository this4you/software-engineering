# Інструкція з встановлення Docker Desktop
## Для студентів

Ця інструкція допоможе встановити Docker Desktop на **macOS** та **Windows**, щоб ви могли запускати локальні середовища для лабораторних робіт.

---

# 🐳 Встановлення Docker Desktop на macOS

## 1. Перевірити сумісність
Docker Desktop підтримує:
- macOS **10.15+** (Catalina, Big Sur, Monterey, Ventura, Sonoma)
- **Apple Silicon (M1/M2/M3)** або **Intel**

## 2. Завантажити Docker Desktop
Перейдіть на офіційний сайт:
👉 https://www.docker.com/products/docker-desktop/

Обери:
- *Mac with Intel chip*  
  або
- *Mac with Apple Silicon*

## 3. Встановити Docker Desktop
1. Відкрий `.dmg` файл.
2. Перетягни Docker у папку **Applications**.
3. Запусти Docker Desktop з Applications.
4. Підтвердь необхідні дозволи (файлова система, мережа).

## 4. Перевірити роботу
Відкрий термінал і введи:

```
docker --version
docker run hello-world
```

Якщо ти бачиш повідомлення **"Hello from Docker!"**, все працює 🎉

---

# 🐳 Встановлення Docker Desktop на Windows

## 1. Вимоги
- Windows **10/11 64-bit**
- Обовʼязково: **WSL 2** (Windows Subsystem for Linux)
- Увімкнена віртуалізація (BIOS → Virtualization Enabled)

## 2. Увімкнути WSL 2

Виконай у PowerShell від імені адміністратора:

```
wsl --install
```

Перезавантаж компʼютер після інсталяції.

## 3. Завантажити Docker Desktop
Завантаж з офіційного сайту:
👉 https://www.docker.com/products/docker-desktop/

## 4. Встановити Docker Desktop
1. Запусти інсталятор.
2. Підтвердь використання **WSL 2 backend**.
3. Дочекайся встановлення.
4. Перезавантаж систему, якщо попросить.

## 5. Перевірити роботу

Відкрий **Command Prompt / PowerShell**:

```
docker --version
docker run hello-world
```

Повідомлення про успішний запуск контейнера означає, що Docker готовий до роботи 🚀

---

# 🎯 Що робити, якщо Docker не запускається?

### На macOS:
- Перевірити, що увімкнено **Rosetta** (для M1/M2 у деяких випадках).
- Перезапустити Docker Desktop.

### На Windows:
- Перевірити, що WSL2 встановлено:
```
wsl -l -v
```
- Увімкнути "Virtualization" у BIOS.

---

# ✅ Готово!
Тепер ви можете переходити до лабораторних робіт, які вимагають Docker 💪🚀
